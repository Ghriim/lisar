# lisar — admin

Back-office: user accounts, reference categories and priorities.

React + TypeScript + Vite, built with the **Ant Design** component library — the admin does not
carry a design of its own.

```
npm install
npm run dev     # http://localhost:5174, /api proxied to http://localhost:8080
```
