# lisar — website

The application users actually use.

React + TypeScript + Vite. Unlike the admin, the website **builds its own design**: no component
library.

```
npm install
npm run dev     # http://localhost:5173, /api proxied to http://localhost:8080
```
